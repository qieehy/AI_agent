[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-agent"
version = "0.1.0"
description = "Production-grade Agent Framework with Runtime, Tool, Memory, RAG, MCP, Multi-Agent"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Qieehy" }]
keywords = ["agent", "llm", "ai", "tool-calling", "rag", "mcp"]

dependencies = [
    "openai>=2.46.0",
    "python-dotenv>=1.2.2",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "tiktoken>=0.7",
    "loguru>=0.7",
    "rich>=13.0",
    "typer>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.1",
    "pytest-mock>=3.12",
    "ruff>=0.4",
    "mypy>=1.10",
    "pre-commit>=3.7",
]
rag = [
    "chromadb>=0.5",
    "faiss-cpu>=1.8",
    "sentence-transformers>=3.0",
]
mcp = [
    "mcp>=1.0",
]
ui = [
    "fastapi>=0.110",
    "uvicorn>=0.27",
]

[project.scripts]
agent = "main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["agent*", "llm*", "memory*", "tools*", "runtime*", "config*", "errors*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
ignore = ["E501"]

[tool.mypy]
plugins = ["pydantic.mypy"]
python_version = "3.10"
ignore_missing_imports = true
warn_return_any = true
warn_unused_configs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short --strict-markers"
markers = [
    "slow: marks tests as slow",
    "integration: integration tests requiring real LLM API",
]
